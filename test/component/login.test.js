const assert = require('assert');
const LOGIN_URL = '/user';

describe('Logging', () => {
    it('should return BAD REQUEST status when given username does not exist', async () => {
        const credentials = {
            username: ''
        }
    });
});
